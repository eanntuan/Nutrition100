Only in ractive-render/lib: escapeUtils.js
diff -r ractive-render-orig/lib/load.js ractive-render/lib/load.js
24a25
> 	console.log("Load load", file);
diff -r ractive-render-orig/lib/ractive-render.js ractive-render/lib/ractive-render.js
21a22
> var escapeUtils = require('./escapeUtils');
54c55,59
< 	templateLoader: 'template'
---
> 	templateLoader: 'template',
> 	delimiters: ['{{', '}}'],
> 	tripleDelimiters: ['{{{', '}}}'],
> 	escapeDelimiters: [],
> 	/*TODO: make escaping delims a setting that is actually checked before it just happens */
120a126,128
> 		options.settings.delimiters = options.delimiters = options.delimiters || rr.settings.delimiters;
> 		escapeUtils.DEFAULT_DELIMS = rr.settings.escapeDelimiters;
> 		// had to move it into options.settings b/c of how utils.buildOPtions selects a subset of the options object to use when building partials and components
125c133,138
< 			return new Component(options).toHTML();
---
> 			// adding unescaping *****************************
> 			var renderedFile = new Component(options).toHTML();
> 			var unescapedRender = escapeUtils.unescapeDelims(renderedFile);
> 			unescapedRender = escapeUtils.unescapeOnEvents(unescapedRender);
> 
> 			return unescapedRender;
diff -r ractive-render-orig/lib/rvc.js ractive-render/lib/rvc.js
31a32
> 	console.log("Rvc load", file);
diff -r ractive-render-orig/lib/template.js ractive-render/lib/template.js
5a6
> var EscapeDelims = require('escape-delims');
10a12
> var escapeUtils = require('./escapeUtils');
27c29,33
< 		return utils.wrap(Ractive.parse(template, options), options);
---
> 		// ************************
>         // Added escaping of template
>         var escapedTemplate = escapeUtils.escapeDelims(template);
>         // ************************
> 		return utils.wrap(Ractive.parse(escapedTemplate /*changed*/, options), options);
38c44
< 
---
> 			
diff -r ractive-render-orig/lib/utils.js ractive-render/lib/utils.js
10a11
> var escapeUtils = require('./escapeUtils');
97c98,101
< 				return Ractive.parse(template, options);
---
> 				// ESCAPE THE PARTIALS AS WELL ***************************
> 				var escapedTemplate = escapeUtils.escapeDelims(template);
> 				escapedTemplate = escapeUtils.escapeOnEvents(escapedTemplate);
> 				return Ractive.parse(escapedTemplate, options);
104c108,111
< 				return Ractive.parse(template, options);
---
> 				// ESCAPE THE PARTIALS AS WELL ***************************
> 				var escapedTemplate = escapeUtils.escapeDelims(template);
> 				escapedTemplate = escapeUtils.escapeOnEvents(escapedTemplate);
> 				return Ractive.parse(escapedTemplate, options);
151a159
> 		console.log("Injecting template");
Only in ractive-render/node_modules: .bin
Only in ractive-render/node_modules: chai
Only in ractive-render/node_modules: escape-delims
Only in ractive-render/node_modules: express
Only in ractive-render/node_modules: mocha
Only in ractive-render/node_modules: ractive
Only in ractive-render/node_modules: ractive-load
Only in ractive-render/node_modules: requirejs
Only in ractive-render/node_modules: rvc
diff -r ractive-render-orig/package.json ractive-render/package.json
21a22
>     "escape-delims": "^0.4.0",
28c29
<     "ractive": "0.5.5",
---
>     "ractive": "0.6.1",
46c47
<   "_from": "ractive-render@^0.3.1",
---
>   "_from": "ractive-render@*",
63,64c64
<   "_resolved": "https://registry.npmjs.org/ractive-render/-/ractive-render-0.3.1.tgz",
<   "readme": "ERROR: No README data found!"
---
>   "_resolved": "https://registry.npmjs.org/ractive-render/-/ractive-render-0.3.1.tgz"
